# SPIKE JETABLE — configuration de build minimale.
# Les dossiers `source` et `resources` sont pris par défaut : rien d'autre
# à déclarer tant qu'on ne fait pas de ressources par-device.
project.manifest = manifest.xml
